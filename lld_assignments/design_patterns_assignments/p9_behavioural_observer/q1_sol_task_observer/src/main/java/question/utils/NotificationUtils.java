package question.utils;

// The NotificationUtils class provides utility methods for sending notifications.
public class NotificationUtils {
    public static void sendEmail(String subject, String content) {
        // Sends an email with the given subject and content
    }

    public static void sendSlack(String subject, String content) {
        // Sends a Slack message with the given subject and content
    }

    public static void sendPush(String subject, String content) {
        // Sends a push notification with the given subject and content
    }
}
